**server side template injection** happens when user input treated as server template but not as plain text before rendering. attacker can manipulate this thing by typing a script into the input feild to make it run in the template.

