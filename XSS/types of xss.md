
## stored XSS 

the data that gets stored on the server and after every refresh it gets there. for example there is an input feild that is receiving a comment and the comment stays there every time.

## reflected XSS

the request is sent on the backend butttt it does not stays there on the website for everytime.


## DOM XSS

request does not send to the backend. it is based on frontend only. as the name shows "DOM" which means document object models. like document.cookie, document.getElementByID('flag').

## payloads

their payloads can be same but can vary due to the logic of each vulnerability.